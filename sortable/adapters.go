package sortable

// type Sortable

// func MergeSort(arr []Sortable) {
// 	temp := make([]sortable, len(arr))
// 	mergeSort(temp)
// 	for i := 0; i < len(arr); i++ {
// 		arr[i] = int(temp[i])
// 	}
// }
